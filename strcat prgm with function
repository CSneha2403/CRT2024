#include <stdio.h>
#include<string.h>
int main() {
    char str1[50],str2[50];
    scanf("%s",str1);
    scanf("%s",str2);
    strcat(str1,str2);
    {
        str2[1]=str1[2];
    }
    printf("%s\n%s",str1,str2);

    return 0;
}
