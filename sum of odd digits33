#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n;
    scanf("%d",&n);
    int sum=n;
    while(n!=-1)
    {
        scanf("%d",&n);
        sum+=n;
    }
    printf("%d",sum+1);
    
    return 0;
}
